type InitArgs = record { id : nat64; creator : principal };
type Metadata = record {
  id : nat64;
  url : opt text;
  creator : principal;
  token : opt principal;
  logo : opt text;
  name : opt text;
};
service : (InitArgs) -> { get_metadata : () -> (Metadata) query }
